console.log("Starting...")
let IsUserSignedIn = true // this denotes that the user is signed in to the app

if (IsUserSignedIn) {
    // this particular block will only execute if the condition is true
    console.log("Show The Dashboard")
}